1. Download the .apk

2. This was another straightforward one. All .apk files are zips.

`unzip simple-app.apk`

`for i in `ls`; do strings $i | grep flag; done`

```
...
flags
flagsFromClass
flag{3asY_4ndr0id_r3vers1ng}
,java.util.regex.Pattern.compile(this, flags)
suggest_flags
...
```

Flag: flag{3asY_4ndr0id_r3vers1ng}

